concept = "A np_thru_hole mounting-hole pad counts as npth (not through_hole) and emits a non-plated tool in the drill file."
doc     = "sexpr-pcb"
input   = "board.kicad_pcb"

# kicad-cli 10.0.5 emits IPC-D-356 record type "367" for this board's np_thru_hole
# (unplated, netless -- net field "N/C") feature, with a `U` plating code where a plated
# hole carries `P` -- visible directly in this case's recorded ipcd356.d356.
