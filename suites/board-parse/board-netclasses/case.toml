concept = "A (net_class ...) that gives one net a tighter clearance than the board default makes DRC report a violation a same-gap pair on two default-class nets would not."
doc     = "sexpr-pcb"
input   = "board.kicad_pcb"
extra   = ["drc", "roundtrip"]
xfail   = "DIV-0004"

# Two 0.2mm F.Cu segments, 0.3mm edge-to-edge gap: satisfies the 0.2mm board default
# but violates NET_A's "Tight" netclass (0.5mm). See docs/FINDINGS.md DIV-0004.
