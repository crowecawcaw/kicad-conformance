concept = "An unrecognised stroke type is rejected with the list of valid ones."
doc     = "sexpr-intro"
input   = "board.kicad_pcb"
control = "control.kicad_pcb"           # same board with (type symbol) replaced by (type default) -> must exit 0
error_contains = "Expecting solid, dash"
