concept = "A (barcode ...) item -- new in KiCad 10 -- is parsed and plots its QR modules on the copper layer it's placed on."
doc     = "sexpr-pcb"
input   = "board.kicad_pcb"

# QR barcode placed directly on F.Cu (not silkscreen): plots as real module geometry
# (paths in render-F_Cu.svg, aperture flashes in the gerber), not an empty code.
