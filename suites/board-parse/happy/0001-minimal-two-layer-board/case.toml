concept = "A minimal two-layer board (no footprints, standard layer table) parses into a mostly-empty but well-formed model."
doc     = "sexpr-pcb"
input   = "board.kicad_pcb"

[[check]]
op       = "model"
expected = "model.json"
