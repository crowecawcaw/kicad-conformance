concept = "A (text_box ...) on a sheet wraps its content to the box's declared width and renders it."
doc     = "sexpr-schematic"
input   = "sheet.kicad_sch"

# One T2 component plus one long text_box, deliberately longer than its 40x25mm
# box, so wrapping is forced, not incidental.
