concept = "A symbol that extends another inherits the parent's graphics and pins, overriding only its own properties."
doc     = "sexpr-symbol-lib"
input   = "lib.kicad_sym"

# "Derived" carries only (extends "Base") plus its own Value property, no body/pin
# sub-symbols of its own; its rendered SVG should still show Base's rectangle and pin.
