concept = "A one-symbol .kicad_sym library exports exactly one SVG, under KiCad's own name."
doc     = "sexpr-symbol-lib"
input   = "lib.kicad_sym"

# Minimal 2-pin "T2" body as a standalone one-symbol library; sym export svg
# should write exactly one file, render/T2_unit1.svg.
