concept = "Each of the nine pin graphic styles draws its own distinct decoration -- bubble, clock wedge, slash, and so on -- in the rendered symbol."
doc     = "sexpr-intro#pin"
input   = "lib.kicad_sym"

# One symbol, "Styles", with nine input pins stacked vertically, one per graphic
# style: line, inverted, clock, inverted_clock, input_low, clock_low, output_low, edge_clock_high, non_logic.
