concept = "A two-unit symbol exports one SVG per unit, with the shared unit-0 body graphics appearing in both."
doc     = "sexpr-symbol-lib"
input   = "lib.kicad_sym"

# "T2U" has a shared unit-0 body (rectangle) plus per-unit pins ("1"/"2"); expect
# separate T2U_unit1.svg / T2U_unit2.svg, each including the shared body.
