concept = "A library holding two symbols exports a render/ directory holding both SVGs, one per symbol."
doc     = "sexpr-symbol-lib"
input   = "lib.kicad_sym"

# "T2" (rectangle body, 2 pins) and "T3" (circle body, 1 pin) in one library;
# render/ must hold both T2_unit1.svg and T3_unit1.svg as a set.
