concept = "The three fill types -- none, outline and background -- draw three visibly different symbol bodies."
doc     = "sexpr-intro#fill"
input   = "lib.kicad_sym"

# Three symbols, "FillNone"/"FillOutline"/"FillBackground", identical rectangle
# bodies differing only in (fill (type ...)).
