concept = "A symbol body drawn with an arc and a bezier (no rectangle) renders both curves in the exported SVG."
doc     = "sexpr-symbol-lib"
input   = "lib.kicad_sym"

# Symbol body is exactly one arc + one bezier (no rectangle), so both curves are
# unavoidably load-bearing in the rendered SVG.
