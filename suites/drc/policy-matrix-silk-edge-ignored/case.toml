concept = "Identical input from silk-matrix-edge-under with only the silk_edge_clearance built-in set to ignore at the project severity layer."
doc = "cli:pcb-drc"
input = "board.kicad_pcb"
extra = ["drc"]
