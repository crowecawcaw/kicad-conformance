concept = "Identical input from courtyard-overlap-paired-controls with only the courtyards_overlap built-in enabled at the project severity layer."
doc = "cli:pcb-drc"
input = "board.kicad_pcb"
extra = ["drc"]
