(version 1)

(rule "narrow"
	(constraint clearance (min 3mm))
	(condition "A.Type == 'pad' && B.Type == 'pad'"))

(rule "wide"
	(constraint clearance (min 5mm))
	(condition "A.Type == 'pad' && B.Type == 'pad'"))
